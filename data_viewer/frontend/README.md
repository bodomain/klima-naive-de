# HYRAS Data Viewer Frontend

Vite/React frontend for the HYRAS data viewer.

From the repository root, prefer:

```bash
./start_app.sh
```

For frontend-only development:

```bash
cd data_viewer/frontend
VITE_API_BASE_URL=http://127.0.0.1:8000 npm run dev
```

Build:

```bash
npm run build
```
